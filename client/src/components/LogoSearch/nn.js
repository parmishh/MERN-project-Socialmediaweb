export const othername=()=> {
    var tt = document.getElementById("poke").value;
    //findString(tt);
    
  }
  /*export function findString (text) {
    //if (parseInt(navigator.appVersion)<4) return;
    var strFound;
    if (window.find) {
   
     // CODE FOR BROWSERS THAT SUPPORT window.find
   
     strFound=self.find(text);
     if (!strFound) {
      strFound=self.find(text,0,1);
      while (self.find(text,0,1)) continue;
     }
    }
  }
  */
  